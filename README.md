# monoptix
MonoptiX is a direct Binary Level Superoptimizer program implementation (also written in binary), that relies on nothing but the input in and of itself, to derive ways to optimize the file in terms of its Time and Space complexity by unprecedented amounts, whilst still preserving all aspects of the original input file.
